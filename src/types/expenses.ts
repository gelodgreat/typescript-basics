export type expensesMeta = {
    description?: string;
    note?: string;
    amount?: number;
    createdAt?: number;
}

export type expensesRequestData = {
    description?: string;
    note?: string;
    amount?: number;
    createdAt?: number;
}